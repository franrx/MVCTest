<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fillViewport="true"
    tools:context=".detail.activities.DetailActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:textColor="@color/comic_cardview_black_text_color"
            android:textSize="@dimen/detail_comic_title_text_size"
            android:text="@string/not_available" />

        <TextView
            android:id="@+id/description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="10dp"
            android:textColor="@color/comic_cardview_black_text_color"
            android:textSize="@dimen/detail_comic_description_text_size"
            android:text="@string/not_available" />

        <FrameLayout
            android:id="@+id/photo_container"
            android:layout_width="@dimen/detail_photo_size"
            android:layout_height="@dimen/detail_photo_size"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="10dp">

            <ImageView
                android:id="@+id/photo"
                android:layout_width="@dimen/detail_photo_size"
                android:layout_height="@dimen/detail_photo_size"
                android:layout_gravity="center"
                android:contentDescription="@null" />

            <ProgressBar
                android:id="@+id/photo_progress"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center" />

        </FrameLayout>


    </LinearLayout>

</ScrollView>
